hostname {{ hostname }}

! Configuration interfaces client{% for name, interface in interfaces.items() %}
interface {{ name }}
 ip address {{ interface.ip_address }} {{ interface.subnet_mask }}
 no shutdown
{% endfor %}
! Configuration eBGP
router bgp {{ BGP_asn }}
 neighbor {{ eBGP_peer.ip_address }} remote-as {{ eBGP_peer.asn }}{% for network in eBGP_advertized_networks %}
 network {{ network.address }} mask {{ network.mask }}{% endfor %}
